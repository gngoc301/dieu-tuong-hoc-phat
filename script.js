const menu=document.querySelector('.menu');const nav=document.querySelector('nav');if(menu&&nav){menu.addEventListener('click',()=>nav.classList.toggle('open'));nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')))}const input=document.querySelector('#librarySearch');if(input){const cards=[...document.querySelectorAll('.book-card')];input.addEventListener('input',e=>{const q=e.target.value.toLowerCase().trim();cards.forEach(card=>card.hidden=q&&!card.textContent.toLowerCase().includes(q))})}